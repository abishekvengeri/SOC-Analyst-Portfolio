# Results Summary

- SQL injection attempts were successfully detected using pattern-based URI matching.
- The event listing search made it easy to see the exact payloads and how the attacker structured the requests.
- The summary view identified which IP addresses sent the most suspicious queries.
- The dashboard visualization provided fast situational awareness for SOC monitoring.
- The saved report created a structured table that can be shared with other analysts or used for ongoing tracking.

This use case demonstrates the ability to detect early signs of web application attacks and transform raw log data into actionable SOC insights.

